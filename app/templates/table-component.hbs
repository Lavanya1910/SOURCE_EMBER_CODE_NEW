<table>
  <thead>
    <tr>
      <th>Column 1</th>
      <th>Column 2</th>
      <!-- Add more column headers as needed -->
    </tr>
  </thead>
  <tbody>
    {{#each @data as |item|}}
      <tr>
        <td>{{item.column1}}</td>
        <td>{{item.column2}}</td>
        <!-- Add more columns as needed -->
      </tr>
    {{/each}}
  </tbody>
</table>
